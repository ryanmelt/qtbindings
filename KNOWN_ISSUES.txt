1. All opengl examples fail because opengl can not be loaded (LoadError)
2. The qRgb macro doesn't appear to work correctly

NOTE: The QDBus examples don't work on Windows because QtDBus is Unix only.
